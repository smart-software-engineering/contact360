defmodule Contact360Web.RegistrationControllerTest do
  use Contact360Web.ConnCase, async: false

  # TODO rewrite this tests completely
  @tag :skip
  test "GET /", %{conn: _conn} do
    # conn = get(conn, ~p"/")
    # assert html_response(conn, 200) =~ "Contact360"
  end
end
